<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.yusrisahrul.lomeccaonlineorder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailHistoryActivity"></activity>
        <activity android:name=".HistoryActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".FilterMenuActivity" />
        <activity android:name=".AddressAddActivity" />
        <activity android:name=".OrderFinishActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".DetailMenuActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".AddressActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>